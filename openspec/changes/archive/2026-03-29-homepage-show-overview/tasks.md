## 1. Type Definitions

- [x] 1.1 Add `overview` field to the `Repository` interface in `types/database.ts`

## 2. Component Implementation

- [x] 2.1 Update `RepoCard.tsx` to conditionally display the `overview` field above the existing descriptions
- [x] 2.2 Truncate the `overview` field to 300 characters and append an ellipsis if necessary
